<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <import type="android.graphics.Color" />

        <import type="android.view.Gravity" />

        <import type="com.smybs0.deepseeklib.entity.ChatRole" />

        <variable
            name="role"
            type="com.smybs0.deepseeklib.entity.ChatRole" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:orientation="horizontal">

        <androidx.cardview.widget.CardView
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:visibility="@{role == ChatRole.ASSISTANT ? View.VISIBLE : View.INVISIBLE}"
            app:cardCornerRadius="15dp">

            <ImageView
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/deepseek" />

        </androidx.cardview.widget.CardView>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="10dp"
            android:layout_weight="1"
            android:gravity="@{role == ChatRole.ASSISTANT ? Gravity.START : role == ChatRole.USER ? Gravity.END: Gravity.CENTER}"
            android:orientation="vertical">

            <View
                android:layout_width="match_parent"
                android:layout_height="0dp" />

            <TextView
                android:id="@+id/tv_reason_content"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="5dp"
                android:background="#ededee"
                android:padding="5dp"
                android:textIsSelectable="true"
                android:textSize="12sp" />

            <com.smybs0.deepseekchat.view.MyMarkdownView
                android:id="@+id/tv_content"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@{role == ChatRole.USER ? Color.parseColor(`#DEF3FE`) : Color.WHITE}"
                android:textIsSelectable="true" />

        </LinearLayout>

        <androidx.cardview.widget.CardView
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:visibility="@{role == ChatRole.USER ? View.VISIBLE : View.INVISIBLE}"
            app:cardCornerRadius="15dp">

            <ImageView
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/person" />

        </androidx.cardview.widget.CardView>

    </LinearLayout>
</layout>